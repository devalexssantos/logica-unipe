/*
Faça um algoritmo para calcular a área de um quadrado. O cálculo
deve ser feito em uma função
Formula: A = L1 x L2
*/

function calcularAreaDoQuadrado(l1, l2) {
    let area = l1 * l2;

    return area;
}

console.log(calcularAreaDoQuadrado(2, 2));
console.log(calcularAreaDoQuadrado(35, 123));